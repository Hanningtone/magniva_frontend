import styled from "styled-components";

interface Props {
    users?: string,
}

const UsersList= (props: Props) => {

    return(
        <div>
            
        </div>
    )
}

export default UsersList;
